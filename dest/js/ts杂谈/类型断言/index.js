var params;
params = '字符串类型';
params = 10;
function getLength(str) {
    if (str.length) {
        return str.length;
    }
    else {
        return str.toString().length;
    }
}
//# sourceMappingURL=index.js.map